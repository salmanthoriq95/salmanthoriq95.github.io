---
import ProjectDetailPage from "../../components/ProjectDetailPage.astro";
import { type Project } from "../../data/projects";
import { getProjectStaticPaths } from "../../lib/staticPaths";

export const getStaticPaths = getProjectStaticPaths;

interface Props {
  project: Project;
}

const { project } = Astro.props;
---

<ProjectDetailPage locale="en" project={project} />
